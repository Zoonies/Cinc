/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'tboyles' at '12/6/14 1:57 AM' with Gradle 2.0
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at http://gradle.org/docs/2.0/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'application'

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'maven central' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    mavenCentral()
}

// In this section you declare the dependencies for your production and test code
dependencies {
    compile 'org.slf4j:slf4j-api:1.7.5'
    compile 'org.jsoup:jsoup:1.8.1'
    compile 'mysql:mysql-connector-java:5.1.6' 
    testCompile 'junit:junit:4.11'
}

task fatJar(type: Jar) {
    manifest {
        attributes 'Implementation-Title': 'Pollen Stream',  
            'Implementation-Version': '0.0.1',
            'Main-Class': 'com.zoonies.cinc.connectors.pollen.Pollen'
    }
    baseName = project.name
    from { configurations.compile.co1llect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}